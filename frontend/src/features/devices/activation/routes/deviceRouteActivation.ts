import type { PageRouteBinding } from "../../../../application/page-activation/routes/pageRouteBinding"

export const deviceRouteActivation:
  PageRouteBinding = {
  pageId: "device.workspace",
  domain: "devices",
  route: "/devices",
  enabled: true,
}
