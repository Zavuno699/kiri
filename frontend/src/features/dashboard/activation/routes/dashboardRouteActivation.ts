import type { PageRouteBinding } from "../../../../application/page-activation/routes/pageRouteBinding"

export const dashboardRouteActivation:
  PageRouteBinding = {
  pageId: "dashboard.overview",
  domain: "dashboard",
  route: "/",
  enabled: true,
}
