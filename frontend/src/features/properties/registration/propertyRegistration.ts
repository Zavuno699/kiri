export interface PropertyRegistration {
  id: "properties"
  registered: boolean
  readOnly: boolean
}

export const propertyRegistration: PropertyRegistration = {
  id: "properties",
  registered: true,
  readOnly: true,
}
