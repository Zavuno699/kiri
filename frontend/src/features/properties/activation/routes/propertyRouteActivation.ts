import type { PageRouteBinding } from "../../../../application/page-activation/routes/pageRouteBinding"

export const propertyRouteActivation:
  PageRouteBinding = {
  pageId: "property.list",
  domain: "properties",
  route: "/properties",
  enabled: true,
}
