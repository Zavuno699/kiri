export type PropertyProjection = {
  id: string;
  status: string;
  leaseCount: number;
  outstandingBalance: number;
  deviceCount: number;
  securityState: string;
  version: number;
  updatedAt: string;
};
